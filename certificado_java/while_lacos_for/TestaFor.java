package certificado_java.while_lacos_for;

public class TestaFor {

	public static void main(String[] args) {
		/*
		 * int j;//pode declarar long x; int numeros[] = new int[]
		 * {1,2,3,4,5,6,7,8,9,0}; for(x= 0, j = 10; x < numeros.length; x++, j--) //so
		 * inicializar a variable. nao pode declerar nas (variaveis diferentes);
		 * 
		 * System.out.println(numeros );
		 */
		// as 3 partes do for são opcionais.
		 
		for(int i = 1; i<10; i++) { //declara e inicializa a variavel=> executa a condição(true or false)> true> executa o loop, depois
			//executa o incremento.
			System.out.println(i); 
		}
		
}
}